# Translation area

No locale is admitted at initialization. Translation records must consume one
exact upstream-plus-overlay release and use stable source identifiers. They may
not modify the upstream mirror or another locale.

