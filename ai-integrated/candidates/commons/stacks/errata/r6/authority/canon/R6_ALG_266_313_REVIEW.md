# ALGEBRA-266–313 independent canon review

The exact frozen authority is `algebra.tex` at commit
`a04446e57ec1fbc252a871afcec7752fb2807b14`, 1,771,230 bytes, 47,885
LF-terminated lines, SHA-256
`FA8BB92E58A4F78A2BD01B3B6A4A87DE0A0D279F5DD90641B574DD5FBFFFA4F3`.
All 48 producer rows occur exactly once in the live French ledger snapshot bound
by `R6_ALG_266_313_INTAKE.json`. No prior canon record uses these producer IDs.

## Accepted units

- **ALGEBRA-266** (`28903–28906`): singular `valuation ring` requires
  `which exists`, not `which exist`.
- **ALGEBRA-267** (`28934–28937`): the chained relatives make `whose kernel`
  attach grammatically to `isomorphism`. A comma alone is insufficient. Recast
  condition (4) as a finite map that is not an isomorphism, has kernel and
  cokernel annihilated by a power of the maximal ideal, and satisfies the two
  remaining conditions.
- **ALGEBRA-268** (`28991–28994`): localization is an isomorphism *at* the
  generic point.
- **ALGEBRA-270** (`29098–29100`): `(insert future reference here)` is literal
  unfinished editorial residue. Delete the placeholder and state the
  maximal-adic completion condition directly; no unidentified citation is
  invented.
- **ALGEBRA-271** (`29114–29116`): the unique DVR expression requires
  `n \in \mathbf Z_{\geq 0}`.
- **ALGEBRA-273** (`29192–29195`): `vectors space` must be `vector space`.
- **ALGEBRA-274** (`29221–29225`): the strict chain omits `\subset` before
  `N_k`.
- **ALGEBRA-275** (`29259–29263`): plural `results` requires `imply`.
- **ALGEBRA-276** (`29311–29318`): insert `a` before `discrete valuation ring`.
- **ALGEBRA-277** (`29363–29364`): the printed definition makes zero a prime
  element because `(0)` is prime in a domain. That makes the multiplicative set
  at lines 29591–29595 contain zero. Require the prime element to be nonzero;
  nonunit status follows because prime ideals are proper.
- **ALGEBRA-278** (`29377`): `A` is undefined; the two multipliers lie in `R`.
- **ALGEBRA-279** (`29374–29379`): cancellation from `x=fgx` is invalid for
  `x=0`, which the lemma allows. Handle `x=0` first, then cancel in the
  nonzero case.
- **ALGEBRA-281** (`29434–29442`): the displayed UFD factorizations do not
  cover zero. Dispose of `a=0` or `b=0`, then use the empty-factorization
  convention for units before choosing irreducible factorizations.
- **ALGEBRA-282** (`29448–29455`): after cancellation the endpoints must be
  `a_2\ldots a_m=u b_2\ldots b_n`; the printed expression swaps `m` and `n`
  and assumes the equality it is proving.
- **ALGEBRA-283** (`29549–29553`): an ascending principal-ideal chain can
  begin with zero, which has no prime factorization. Handle the all-zero chain
  or truncate its initial zero segment before factoring.
- **ALGEBRA-284** (`29564–29574`): the polynomial analogue likewise needs its
  initial zero segment handled before degree and leading coefficient are used.
- **ALGEBRA-285** (`29589–29593`): the cited factorization lemma applies to
  nonzero nonunits. Add that qualifier to both assertions.
- **ALGEBRA-286** (`29606–29610`): zero is integral but cannot be written as a
  unit times powers of irreducibles. Handle `x=0` first.
- **ALGEBRA-288** (`29699–29704`): the zero prime equals its square, so the
  printed choice from `\mathfrak p\setminus\mathfrak p^2` fails. Dispose of
  `(0)` as finitely generated, then take a nonzero prime.
- **ALGEBRA-290** (`29727–29729`): the sentence mixes free variables
  `\mathfrak m` and `\mathfrak p` and incorrectly includes the zero prime.
  Match condition (2): every nonzero maximal ideal `\mathfrak m`.
- **ALGEBRA-291** (`29735–29738`): `I=R` is not prime and lies in no maximal
  ideal, while zero is outside the required factorization scope. Treat the unit
  ideal as the empty product and then take `I` nonzero and proper.
- **ALGEBRA-297** (`29441–29442`): the associate list omits `a_n`. It must be
  `a_1,\ldots,a_n,b_1,\ldots,b_m`.
- **ALGEBRA-298** (`29868–29875`, `29937–29942`): six quotient expressions
  need grouping to denote the typed modules: two each of `M/(M\cap M')` and
  `M'/(M\cap M')`, plus `(M+M')/M'` and `(M+M')/M`.
- **ALGEBRA-299** (`29996–30000`): the outer `d(` is unmatched. The smallest
  correction inserts one closing parenthesis before `&`; harmless inner
  grouping is retained.
- **ALGEBRA-300** (`30020–30034`): `b` is undefined; the ambient rank is `n`.
- **ALGEBRA-301** (`30334–30336`): insert `an` in `an Artinian ring`.
- **ALGEBRA-302** (`30381–30386`): insert the missing determiner, yielding
  `the stability of the finite type property under base change`.
- **ALGEBRA-303** (`30432–30444`): only `P_i` is defined; both nilpotence
  relations must be `P_i(x_i)^{e_i}=0`.
- **ALGEBRA-304** (`30483–30495`): for `n=0` the proof's exponent `n-1` is
  `-1`. Handle `n=0` and then assume `n\geq1`.
- **ALGEBRA-305** (`30488–30491`): multiply the equation *by*, not *with*,
  the coefficient power.
- **ALGEBRA-306** (`30570–30580`): state that `\varphi(R)` *is* integrally
  closed in `S`.
- **ALGEBRA-307** (`30713–30718`): `Assume ... be` must be `Assume ... is`.
- **ALGEBRA-308** (`30754–30756`): delete the duplicated first copula in
  `The base case is m=0 is vacuous`.
- **ALGEBRA-309** (`30759–30767`): the cited lemma only gives integrality over
  `S'`. In both cases state transitivity through `S'/R`, then use the
  definition of `S'` as the integral closure to conclude membership.
- **ALGEBRA-310** (`30778–30785`): an arrow is not an `R`-algebra. State that
  `S` is a finite-type `R`-algebra.
- **ALGEBRA-311** (`30789–30793`): the enclosing result is a theorem, not a
  proposition.
- **ALGEBRA-312** (`30789–30793`): the certain smallest correction is the
  missing comma in `(For example, generators of S over R.)`; a nominal
  parenthetical need not acquire the producer's optional verb `take`.
- **ALGEBRA-313** (`30859–30861`): give both fragments finite predicates:
  surjectivity follows from the chosen generators; injectivity follows from
  the inclusion and exactness of localization.

## Rejected producer claims

- **ALGEBRA-272**: unindexed associated-graded notation and
  `A/\mathfrak m[T]` are established contextual shorthand. The parenthesization
  claim repeats the already rejected ALGEBRA-208 class.
- **ALGEBRA-280**: from a nonfactorable `x`, factor one nonfactorable factor
  again and combine the other factor with the remaining original factor; both
  resulting factors can indeed be chosen nonunit and nonirreducible.
- **ALGEBRA-287**: the printed sentence is valid standard shorthand from a UFD
  factorization of a generator to the corresponding product of principal
  prime ideals.
- **ALGEBRA-289**: in the local ring, `by` is in the maximal ideal, so `1-by`
  is a unit. The printed inference is correct; expansion is optional.
- **ALGEBRA-292**: strictness follows by localizing at `\mathfrak p` and
  comparing `(\pi^n)` with `(\pi^{n-1})`. The induction is valid.
- **ALGEBRA-293**: the source explicitly says the uniqueness proof is omitted;
  that is not a false assertion. Localization and DVR exponents supply it.
- **ALGEBRA-294**: `Dividing` has its standard successive meaning. Repeated
  prime factors are cancelled one occurrence at a time from the updated
  equality.
- **ALGEBRA-295**: the standard empty-product convention already covers the
  unit ideal; no positive lower bound on the number of factors is printed.
- **ALGEBRA-296**: `L` is a field and hence normal; the cited integral-closure
  results already make `B` normal. An extra citation is optional exposition.

## Unresolved, not admitted

- **ALGEBRA-269** (`29072–29078`) correctly identifies a proof gap: the cited
  lemma assumes a local ring, while the remark assumes only semilocal `R`.
  Applying the lemma to `R_{\mathfrak m}` does not itself construct the claimed
  finite global extension. The producer offers two materially different
  repairs, and the global blowup route still needs a proof or exact citation of
  finiteness and nontriviality. Under the errata proof standard, no unique
  correction is admitted yet.

## Closure

The disposition is 38 accepted units, 9 rejected claims, and 1 unresolved
candidate, represented by 46 exact operations. Accepted stable IDs are
`MC-STK-ERR-0621` through `MC-STK-ERR-0658` in ascending producer-ID order.
The immutable authority and all canonical translation bytes remain unchanged;
only the English AI derived candidate may receive the accepted operations.
