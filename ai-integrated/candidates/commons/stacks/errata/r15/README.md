# Stacks errata candidate R15

This standalone candidate records eight independently adjudicated source corrections, implemented as eight exact operations against Stacks Project commit `a04446e57ec1fbc252a871afcec7752fb2807b14` (tree `3feeb703b931a6e7259782c10e7d1575adc83e5e`). It is an independently maintained, AI-produced candidate for **Stacks — English AI Edition**. The Stacks Project authors and maintainers have not requested, reviewed, approved, or endorsed it.

The payload changes only `brauer.tex` and is derived directly from the frozen authority, never from a prior candidate payload. BRAUER-002 through BRAUER-006 and BRAUER-008 through BRAUER-010 are accepted as `MC-STK-ERR-0745` through `MC-STK-ERR-0752`. BRAUER-001 and BRAUER-007 are retained as `rejected_duplicate` evidence because their exact corrections are already admitted as `MC-STK-ERR-0016` and `MC-STK-ERR-0017` in R2. Canonical translations, the literal upstream mirror, permanent tags, and unrelated chapters remain unchanged.

Exact authority bytes, source-span maps, adjudication, two-build PDF determinism evidence, page-complete visual QA, and independent replay are retained beside the payload. The shared producer ledger was used only to locate allegations; `BRAUER_SOURCE_EMENDATIONS.json` was not opened, imported, or trusted as canon. Upstream content remains governed by GNU Free Documentation License 1.2; exact licence bytes are in `authority/COPYING`.

This directory is reserved by `stacks-lease-000018-errata-r15`. A lease and a passing candidate are not overlay admission; admission requires a separate, receipt-bound transition.
