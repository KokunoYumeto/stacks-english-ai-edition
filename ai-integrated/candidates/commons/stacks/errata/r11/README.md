# Stacks errata candidate R11

This standalone candidate records four independently adjudicated source corrections, implemented as five exact operations against Stacks Project commit `a04446e57ec1fbc252a871afcec7752fb2807b14` (tree `3feeb703b931a6e7259782c10e7d1575adc83e5e`). It is an independently maintained, AI-produced candidate for **Stacks — English AI Edition**. The Stacks Project authors and maintainers have not requested, reviewed, approved, or endorsed it.

The payload changes only `algebra.tex` and is derived directly from the frozen authority, never from a prior candidate payload. All four producer rows are accepted. Canonical translations, the literal upstream mirror, permanent tags, and unrelated chapters remain unchanged.

Exact authority bytes, producer intake, source-span maps, adjudication, two-build PDF determinism evidence, page-complete visual QA, and independent replay are retained beside the payload. Upstream content remains governed by GNU Free Documentation License 1.2; exact licence bytes are in `authority/COPYING`.

This directory is reserved by `stacks-lease-000014-errata-r11`. A lease and a passing candidate are not overlay admission; admission requires a separate, receipt-bound transition.
