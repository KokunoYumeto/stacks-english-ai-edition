# Verified Stacks errata candidate R4

This candidate records 154 independently proved source corrections against
Stacks Project commit `a04446e57ec1fbc252a871afcec7752fb2807b14` (tree `3feeb703b931a6e7259782c10e7d1575adc83e5e`). It is an independently
maintained, AI-produced candidate for **Stacks — English AI Edition**. The
Stacks Project authors and maintainers have not requested, reviewed, approved,
or endorsed it.

The payload changes only `topology.tex`, `sheaves.tex`, and `sites.tex`.
Every change is reconstructed from an exact half-open UTF-8 authority span;
sixteen hypotheses are rejected and two later reports are retained only as
duplicate aliases. Canonical translations, the literal upstream mirror,
permanent tags, and unrelated chapters are unchanged.

The exact authority bytes, producer evidence, independent proof reviews,
counterexample replay, source map, decisions, rejections, formula/prose
inventory, authority-versus-candidate builds, visual QA, and independent replay
are retained beside the payload. Upstream content remains governed by GNU Free
Documentation License 1.2; exact licence bytes are in `authority/COPYING`.

This directory is reserved by `stacks-lease-000007-errata-r4`. A lease and a passing candidate are
not overlay admission; admission requires a separate receipt-bound transition.
