# Stacks errata candidate R23

R23 contains ten independently adjudicated corrections to `more-algebra.tex` at Stacks Project commit `a04446e57ec1fbc252a871afcec7752fb2807b14` (tree `3feeb703b931a6e7259782c10e7d1575adc83e5e`). Stable IDs `MC-STK-ERR-0998..1007` continue after the frozen R22 range without changing any R22 assignment.

The thirteen H/J producer rows close as ten accepted semantic units, twelve exact operations, and one rejected notation-only allegation. J-003/J-004 and J-007/J-008 are merged in pairs; J-012 is ordered by its earlier physical locus. Ten prior P02 records are retained only as aliases.

`materialize.py` derives the payload directly from the frozen authority, maps every exact UTF-8 operation, and emits the established candidate schemas. `verify-source.py` independently replays the payload and checks source topology, operation closure, evidence identities, ledger closure, and public hygiene.

This directory is candidate material only. It does not admit an overlay, change the registry, compose a generated-source branch, push Git state, or publish a release. Its lease pointer is bound to active registrar-issued lease `stacks-lease-000027-errata-r23`.

The Stacks Project authors and maintainers have not requested, reviewed, approved, or endorsed this independently maintained AI-produced candidate. Upstream content remains under GNU Free Documentation License 1.2.
